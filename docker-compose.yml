version: "3.9"

services:
  backend:
    build: ./backend
    container_name: fastapi_app
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: streamlit_app
    ports:
      - "8501:8501"
    depends_on:
      - backend
